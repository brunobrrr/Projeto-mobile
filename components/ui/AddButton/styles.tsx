import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignSelf: 'center',
        position: 'absolute',
        bottom: 40,
        backgroundColor: '#BF2B0A',
        padding: 15,
        borderRadius: 30
    }
})